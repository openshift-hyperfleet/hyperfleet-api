# Cluster Status Payload Template
# Used for reporting cluster status back to HyperFleet API
status: "{{ .status }}"
message: "{{ .message }}"
observed_generation: "{{ .eventGenerationId }}"
last_updated: "{{ now | date \"2006-01-02T15:04:05Z07:00\" }}"
conditions:
  - type: "Reconciled"
    status: "{{ .reconciledStatus | default \"Unknown\" }}"
    reason: "{{ .reconciledReason | default \"Pending\" }}"
    message: "{{ .reconciledMessage | default \"Cluster status is being determined\" }}"
  - type: "Configured"
    status: "{{ .configuredStatus | default \"Unknown\" }}"
    reason: "{{ .configuredReason | default \"Pending\" }}"
    message: "{{ .configuredMessage | default \"Configuration is being applied\" }}"

